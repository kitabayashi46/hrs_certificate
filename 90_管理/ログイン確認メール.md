以下ご確認頂けますでしょうか？

1. 「3-1ログイン」APIのパラメータは、記載されているメールアドレス・パスワードではなく、新ヒロセWebから渡される認証用ハッシュコードの認識です。  
2. 環境証明書システムのレンダリングはSPAではなくSSRとなります。  
   つまり画面要求時にサーバ側でログインやセッション確認APIを呼びます。  
   そのため、各APIはCookieではなく、ログインで取得したtokenをパラメータで渡すようにAPIドキュメントの修正をお願いしたいです。  
3. APIのうち「3-2 ログアウト」「3-4 情報取得」は、環境証明書システムでは使うケースは無い認識です。  
4. セッションが無効な場合の振る舞いは未検討でした。  
   セッション切れ画面を表示し、その画面のOKを押すと画面を閉じる、で良いでしょうか？

---

認証方式の概要が記載されている添付ファイルの1ページに「AAからアーキテクチャを連絡する」とありますが、  
新環境証明書WebはSSR、つまりバックエンドからのリクエストとなります。

つまりセッションなども新環境証明書Web側で制御可能なので、ログインAPIのみあれば問題ありません。  
ログインAPIのIFは以下のような想定です。

## ログインAPI IF想定

### リクエストパラメータ
- 証明書Web認証用ハッシュコード（添付資料参照）

### レスポンス
- ユーザID
- 名前
- メールアドレス

### エラーレスポンス
- APIドキュメントに記載されているまま

---

まだ新Web側のベンダーは実装には着手していないと思われますので、  
こちらからの要望を述べさせて頂きました。

新環境証明書Webは新Webとは別のサーバ、アーキテクチャで動作するシステムですので、  
ログインAPIのみで極力疎結合であることが望ましいかと思います。

この件についてお打ち合わせ必要であればお知らせください。